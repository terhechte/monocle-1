<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="keywords" content="swift,ios,apple,linux,objective-c,swiftlang,macosx,ipad,iphone,ipad,applewatch,appletv">
  <meta name="description" content="<%= (@post.summary || 'SwiftWatch').truncate(150) %>">

  <title><%= @post.title %></title>
</head>
<body>
  <h1><a href="<%= @post.url %>"><%= @post.title %></a></h1>

  <p>
    <%= @post.summary %>
    <a href="<%= @post.url %>">Read more</a>
  </p>

  <% @post.comments.each do |comment| %>
    <section>
      <header><%= comment.user_handle %></header>
      <time><%= comment.created_at.to_s %></time>
      <%== comment.formatted_body %>
    </section>
  <% end %>
</body>
</html>
